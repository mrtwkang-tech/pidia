# Vercel project link and the OIDC token it writes
.vercel
.env*

# Build outputs that are not the site: the self-contained single-file page is
# 1.3 MB of inlined fonts and three.js, regenerated by build.py on demand.
_deploy/

# Local tooling state
.gstack/
.claude/
__pycache__/
*.pyc
